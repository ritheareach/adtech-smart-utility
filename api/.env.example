# Server
PORT=3000
NODE_ENV=development

# PostgreSQL
DB_HOST=localhost
DB_PORT=5432
DB_NAME=adtech
DB_USER=postgres
DB_PASSWORD=your_password

# JWT
JWT_SECRET=your_super_secret_jwt_key_change_this_in_production
JWT_EXPIRES_IN=7d

# OTP
OTP_EXPIRES_MINUTES=5
# Set to true in development to return OTP in API response (never use in production)
OTP_DEV_MODE=true
